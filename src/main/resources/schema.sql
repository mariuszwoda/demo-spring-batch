CREATE TABLE file_table (
                            rec_id BIGINT PRIMARY KEY,
                            name VARCHAR(255),
                            date DATE,
                            file_name VARCHAR(255)
);
